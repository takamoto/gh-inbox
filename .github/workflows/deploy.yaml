name: 'Deploy'

on:
  push:
    tags:
      - '*'

jobs:
  deploy:
    environment:
      name: test

    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3.0.0
      - run: export SECRET="${{ secrets.SECRET_TEST }}"
      - run: echo "${SECRET}"
